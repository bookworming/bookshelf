local protocol = "https://"
local subdomain = "raw."
local domain = "githubusercontent.com"
local name = "/bookworming"
local repository = "/bookshelf"
local directory = "/refs/heads"
local branch = "/main"

local book = "/book%202"
local file = "/%CA%94l.txt"

local link = protocol .. subdomain .. domain .. name .. repository .. directory .. branch .. book .. file